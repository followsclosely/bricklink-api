subprojects {

    group = 'io.github.followsclosely'
    version = '1.0.0'

    repositories {
        mavenCentral()
        mavenLocal()
    }

    plugins.withType(JavaPlugin) {
        java {
            toolchain {
                languageVersion = JavaLanguageVersion.of(17)
            }
        }
    }

}
